---
import { url } from "../lib/url";
---

<div
  id="sticky-mobile-cta"
  class="lg:hidden fixed bottom-0 left-0 right-0 z-30 px-3 pb-3 pt-3 bg-white/95 backdrop-blur-sm border-t border-warm-100 shadow-[0_-2px_12px_rgba(0,0,0,0.06)] transition-transform duration-300 translate-y-full"
  inert
>
  <div class="grid grid-cols-[auto_1fr] gap-2 max-w-md mx-auto">
    <a
      href="tel:+33232333193"
      class="inline-flex items-center justify-center gap-2 px-4 py-3.5 rounded-md border border-ink-200 bg-white text-ink-900 font-bold text-sm"
      aria-label="Appeler le 02 32 33 31 93"
    >
      <svg class="w-5 h-5 text-brand-700" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
      </svg>
      Appeler
    </a>
    <a
      href={url("/contact")}
      class="inline-flex items-center justify-center gap-2 px-4 py-3.5 rounded-md bg-brand-600 text-white font-bold text-sm hover:bg-brand-700 transition-colors"
    >
      Demander un devis gratuit
    </a>
  </div>
</div>

<script is:inline>
  const cta = document.getElementById("sticky-mobile-cta");
  if (cta) {
    let ticking = false;

    function onScroll() {
      const y = window.scrollY;
      const showThreshold = 600;
      const footer = document.querySelector("footer");
      const footerTop = footer ? footer.getBoundingClientRect().top + window.scrollY : Infinity;
      const nearFooter = y + window.innerHeight > footerTop - 50;

      if (y > showThreshold && !nearFooter) {
        cta.classList.remove("translate-y-full");
        cta.removeAttribute("inert");
      } else {
        cta.classList.add("translate-y-full");
        cta.setAttribute("inert", "");
      }

      ticking = false;
    }

    window.addEventListener(
      "scroll",
      () => {
        if (!ticking) {
          window.requestAnimationFrame(onScroll);
          ticking = true;
        }
      },
      { passive: true }
    );
  }
</script>
